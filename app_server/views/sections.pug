extends layout

block content
  .row
    .h1.pageTitle="My pretrip"
  .row.g-2
    each section in sections
      div.section
        a.sectionTitle.h3(href=`/sections/${section._id}`)= section.title
